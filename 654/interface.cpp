#include "pch.h"
using namespace std;
/*
void menu(user currentUser) {
	while (1) {
		cout << "1. QUAN LY NGUOI DUNG" << endl;
		cout << "2. QUAN LY DOC GIA" << endl;
		cout << "3. QUAN LY SACH" << endl;
		cout << "4. QUAN LY MUON TRA SACH" << endl;
		cout << "5. THONG KE" << endl;
		cout << "HAY CHON CONG VIEC: ";
		int choose = 0;
		fflush(stdin);
		cin >> choose;
		switch (choose)
		{
		case 1: UserManager(p, u); break;
		case 2: MemberManager(p); break;
		case 3: BookManager(p); break;
		case 4: IssueManager(p); break;
		case 5: Report(p); break;
		case 0: return 0;
		default: break;
		}
	}
}
*/
